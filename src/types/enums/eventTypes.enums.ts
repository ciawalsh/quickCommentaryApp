enum EventTypes {
  YELLOW_CARD = 'yellow_card',
  RED_CARD = 'red_card',
  MISSED_CHANCE = 'missed_chance',
  GOAL = 'goal',
  SAVE = 'save',
  UPDATE = 'update',
  PENALTY = 'penalty',
}

export default EventTypes;
